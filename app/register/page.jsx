import React from 'react'
import Register from './Register';

const page = () => {
  return <Register />
}

export const metadata = {
  title: "Register",
  description: "Login to use",
};

export default page
