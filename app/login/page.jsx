import React from 'react'
import Login from './Login';

const page = () => {
  return <Login />
}

export const metadata = {
    title: "Login",
    description: "Login to use",
  };
export default page